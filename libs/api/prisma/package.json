{
  "name": "prisma-module",
  "author": "Nils Boehm",
  "license": "MIT",
  "scripts": {
    "prisma:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio",
    "db:rm": "docker compose rm dvs-db-dev -s -f -v",
    "db:up": "docker compose up dvs-db-dev -d",
    "db:restart": "yarn db:rm && yarn db:up && sleep 1 && yarn prisma:deploy && yarn prisma:studio",
    "db:test:rm": "docker compose rm dvs-db-test -s -f -v",
    "db:test:up": "docker compose up dvs-db-test -d",
    "db:test:restart": "yarn db:test:rm && yarn db:test:up && sleep 1 && yarn prisma:deploy"
  }
}
